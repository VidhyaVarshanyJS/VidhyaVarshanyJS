name: "📚 Blog Updater"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # Runs Every Day

jobs:
  update_blogs:
    name: "Update Blogs"
    runs-on: ubuntu-latest
    steps:
      - name: "📥  Fetching Repository Contents"
        uses: actions/checkout@main

      - name: "📚  Hashnode Updater"
        uses: "varunsridharan/action-hashnode-blog@main"
        with:
          USERNAME: 'vidhyavarshany' # Hashnode Username
          BLOG_URL: 'https://vidhyavarshany.hashnode.dev' # Blog URL
          COUNT: 3 # MAX Visisble
          STYLE: "blog-grid"
        env:
          GITHUB_TOKEN: ${{ secrets.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 }}
